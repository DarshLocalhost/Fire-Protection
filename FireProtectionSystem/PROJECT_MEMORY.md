# PROJECT_MEMORY.md — FireProtectionSystem

> **Authoritative, compact working memory for future Claude Code / AI sessions.**
> Read THIS first (then [[STANDARDS_MEMORY]]). Do **not** reread the whole repo or the two PDFs to
> re-establish state — that is what this file is for. Snapshot date: **2026-08-25**.
>
> **Confidence labels used throughout:** `PROVEN` (evidence exists) · `LIKELY` · `SUSPECTED` ·
> `UNVERIFIED` · `BLOCKED`. **Verification tiers:** `BUILD-VERIFIED` · `TEST-VERIFIED` ·
> `STATIC-VERIFIED` (provable by reading code) · `RUNTIME-VERIFIED` (run in Revit) · `UNVERIFIED`.
> Rule: never upgrade a hypothesis to fact; never claim Revit runtime success from a compile alone;
> never invent a fire-protection rule (see [[STANDARDS_MEMORY]]).

---

## 0. READ-ME-FIRST: current truth vs stale docs (critical)

This session (2026-08-25, "memory compaction") **verified the actual source code** against the tracking
docs and found **documentation drift**:

- ✅ **CURRENT / matches code:** `DECISIONS.md` (Decision **011**), `ARCHITECTURE_AUDIT.md`,
  `MIGRATION_PLAN.md`, `RISK_REGISTER.md`, `STANDARDS_COMPLIANCE_MATRIX.md`. (Latest edits ~17:2x.)
- ⚠️ **STALE on the placement fix (do not trust their `file:line` refs):** `PROGRESS.md` ("In Progress"),
  `TODO.md` (P0 + the `[x]` fix item), `SESSION_NOTES.md` (last two entries), and
  `SPRINKLER_ACTUAL_Z_DIAGNOSTIC.md`. These describe the **superseded Decision 010** "WorkPlaneBased→Level
  *fallback*" and cite lines like `RevitSprinklerPlacementService.cs:254-263` / `isFaceBased` at `~84`.
  **That code no longer exists.** The current service uses an explicit **strategy pattern** (Decision 011).

When those files disagree with this one about placement, **this file + Decision 011 + the actual source
win.** The stale files are kept for history (hard rule 16) — they are still correct about the *runtime
(0,0,0) root cause*, only wrong about the *current fix shape*.

---

## 1. Project Identity

- **Name:** FireProtectionSystem — a **Revit add-in** for fire-protection planning.
- **Purpose:** Read the active Revit MEP model + linked architectural models → extract fire-protection
  data (levels, rooms+boundaries, ceilings, obstacles, existing sprinklers) into a normalized host-MEP
  JSON snapshot → calculate sprinkler positions ("BruteForce") → place real Revit `FamilyInstance`
  sprinklers → report requested-vs-actual results.
- **Stack:** C# / .NET, **WPF** (UI), **Revit API** (version-specific `HintPath`), **Newtonsoft.Json 13.0.4**.
  `ImplicitUsings` **disabled**; `LangVersion 7.3`.
- **Revit versions / TFMs:** Revit **2024 → `net48`**; Revit **2025 / 2026 → `net8.0-windows`** (all x64).
  Build configs: `Debug`, `Release`, `Revit2024`, `Revit2025`, `Revit2026`. Revit 2024/2025/2026 all
  installed on this machine.
- **Solution:** `FireProtectionSystem.slnx`.
- **Projects (3):**
  - `FireProtection.Backend` — Revit-heavy: extraction, calc engine, placement adapter, ribbon/command.
  - `FireProtection.UI` — **Revit-free** WPF MVVM: Views, ViewModels, UI DTOs, service *interfaces*.
  - `FireProtection.Tests` — **Revit-free** console harness (14 logic-only tests), **not** a formal test fmwk.

## 2. Current Architecture

```
FireProtection.UI       (WPF, Revit-FREE)  — Views, ViewModels, UI DTOs, service interfaces
      ▲
      │ ProjectReference  (Backend → UI ONLY; UI never references Backend)
      │
FireProtection.Backend  (Revit-heavy)      — extraction, calc engine, placement strategies, command
FireProtection.Tests    (Revit-FREE)       — 14-test logic harness
```

- **Dependency direction (PROVEN, STATIC):** `Backend.csproj` references `UI`; UI has no back-ref
  (`RevitSprinklerPlacementService.cs` uses `FireProtection.UI.Models…` / `.Services`). Contracts
  (`ISprinklerPlacementService`, `ISprinklerFamilySource`, `IPlacementInputExporter`) live in **UI**;
  concrete Revit services are built in `FireProtectionCommand` and injected via
  `UiLauncher → MainWindow → MainWindowViewModel → SprinklerViewModel → SprinklerBruteForceViewModel`.
- **Layer isolation (STATIC-VERIFIED by audit):** calc engine + consumed DTOs are 100% Revit-free (Revit
  `ElementId`s carried as `string`); UI is Revit-free; code-behind is thin (`InitializeComponent` only).
- **Entry point:** `Revit/FireProtectionApplication : IExternalApplication` builds the "Fire Protection"
  ribbon; the button targets `Commands/FireProtectionCommand : IExternalCommand`
  (`[Transaction(Manual)]`). The `.addin` (generated by the `DeployToRevit` target) registers **only** the
  Application add-in (Decision 006).

## 3. Standards Authority (summary — details in [[STANDARDS_MEMORY]])

- **PDF 1 = `72-19-PDF 1.pdf`** → **NFPA 72 (2019)**, *National Fire Alarm and Signaling Code*, ~419 pp,
  **text-extractable**. Cache: `.analysis/pdf/pdf1_layout.txt` (37,198 lines). Relevant to the (not-yet-built)
  **alarm / smoke-detector / notification-appliance** workflows. **No sprinkler spacing tables** (72 is alarm).
- **PDF 2 = `Layout, Detail, And Calculations of Fire Sprinkler Systems (1).pdf`** → sprinkler-layout
  textbook, ~682 pp, ~277 MB, **image-only scan, NO OCR available**. This is the sprinkler-relevant source
  but is **inaccessible** in this environment. **NFPA-13 spacing tables are therefore NOT encoded.**
- **CRITICAL:** No fire-protection engineering rule is confirmed/encoded from either PDF. **All sprinkler
  spacing = provisional 15 ft placeholder** (Decision 004; `HasApprovedRules=false`, every room
  `ReviewRequired`). Any hazard-specific/NFPA-13 numeric value is **UNVERIFIED — DO NOT IMPLEMENT AS A
  DESIGN RULE** until an FPE supplies approved tables. Do **not** substitute general NFPA memory.

## 4. Sprinkler Pipeline (as-built, STATIC-VERIFIED)

| # | Stage | Class · method | In → Out | Notes |
|---|---|---|---|---|
| 1 | Extraction | `FireProtectionExtractionService` → `Services/Model/*Extractor` | Revit docs → `ModelSnapshot` | read-only, **zero-transaction**; links transformed once |
| 2 | ↳ levels/rooms/ceilings/obstacles/existing | `LevelExtractor`, `RoomExtractor`, `CeilingExtractor`, `ObstacleExtractor`, `ExistingSprinklerExtractor` | — | hazard tag via `HazardClassifier.ClassifyByName` |
| 3 | Validate | `ModelExtractionValidator` | snapshot → issues | non-aborting |
| 4 | Export snapshot | `JsonSnapshotExporter` | → `ModelSnapshot_*.json` | UI deserializes into `FireProtectionUiData` |
| 5 | Placement input | `PlacementInputBuilder.Build` | UI selections → `PlacementInputSnapshot` | **in-memory; never reads a JSON file** |
| 6 | Calculation | `BruteForceCalculationService.Calculate` | snapshot → `BruteForceCalculationResult` (`RoomCalculationResult` → `CalculatedSprinklerPoint{X,Y,Z,RoomId,LevelId,LevelName}`) | 1 ft grid, greedy ~15 ft packing, Z from ceiling/level |
| 7 | Level resolution | `RevitSprinklerPlacementService.ResolveHostLevel` | levelId/name → host `Level` | host id → else linked-level elevation/name mapping (Decision 007) |
| 8 | Family resolution | `RevitSprinklerPlacementService.ResolveSymbol` | family/type name → `FamilySymbol` | by name over `OST_Sprinklers`; never hard-coded id |
| 9 | Strategy select + place | `PlaceSinglePoint` → `IFamilyPlacementStrategy.Place` (Face/WorkPlane/Level) + `CeilingHostResolver` | point → `PlacementOutcome` | **Decision 011**; see §7 |
| 10 | Post-placement validation | `PlaceSinglePoint` (§23 block) | instance → `PLACED_AND_VALID`/`PLACED_BUT_INVALID` | reads `instance.Location`; delta vs requested |
| 11 | Result + export | `SprinklerPlacementResult` (+ per-room/point) · `PlacementInputJsonExporter.ExportPlacementResult` | → timestamped JSON | requested **and** actual XYZ recorded |

Calc-engine files: `…/Final/BruteForce/` = `BruteForceCalculationService`, `DefaultHazardPlacementRules`,
`IHazardPlacementRules`, `HazardPlacementRuleSet`, `BruteForceCalculationConfig`, `GeometryMath`,
`RoomGeometry`. Placement files: `…/Final/RevitSprinklerPlacementService.cs`,
`…/Final/Strategies/{PlacementStrategyContracts, CeilingHostResolver, FaceBasedPlacementStrategy,
WorkPlaneBasedPlacementStrategy, LevelBasedPlacementStrategy}.cs`, `…/Final/PlacementInputBuilder.cs`,
`…/Final/PlacementInputJsonExporter.cs`. Models: `…/Final/{PlacementInputSnapshot, PlacementRoomInput,
SelectedSprinklerInfo}.cs`; UI result models in `FireProtection.UI/Models/Sprinklers/BruteForce/`.

## 5. Coordinate System (STATIC-VERIFIED)

- **One system:** host-MEP coordinate space, **feet**. All linked geometry is transformed to host space
  **exactly once at extraction** via `RevitModelContext.TransformPoint/TransformVector/TransformBoundingBox`
  (bbox transforms all 8 corners — correct). Downstream calc/placement **do not re-transform**
  (Decisions 002, 008). **No double-transform found** (audit).
- **Where XYZ is generated:** `BruteForceCalculationService` (X/Y grid + Z, all host-space feet) →
  `CalculatedSprinklerPoint`.
- **At placement:** the requested `XYZ = new XYZ(point.X, point.Y, point.Z)` is passed to Revit
  **unchanged**. The **only** transform at placement is the *search* point mapped into link space
  (`linkInstance.GetTotalTransform().Inverse`) inside `CeilingHostResolver` to find linked ceiling geometry.
- **REQUESTED vs ACTUAL XYZ:** both are recorded. `PlacedSprinklerEntry.RequestedX/Y/Z` = calc input;
  `.X/Y/Z` = **actual `instance.Location`** read after creation; `.PlacementDeviationFt` = distance between
  them. This is the honest diagnostic model the master prompt asks for — **keep it**.
- **Caveat (SUSPECTED/convention-only):** the single-transform invariant is enforced by convention, not a
  host-space wrapper type; nothing structurally stops a future re-transform (R-04).

## 6. Sprinkler Z Logic (as-built; per audit + Z-diagnostic, STATIC)

`placementZ` in `BruteForceCalculationService`:
- **Flat ceiling present:** `Z = flatCeiling.BottomElevationFt`. The FLAT ceiling is picked with an
  **unsorted `FirstOrDefault`** → arbitrary when >1 FLAT ceiling (bug **C-A**, audit `…Service.cs:114-127`).
- **Missing/sloped/stepped/unsupported ceiling:** fallback `Z = LevelElevationFt + CeilingHeightFt`, room
  flagged (`MissingCeiling`/`UnsupportedCeiling`). **Concern C-G:** that ceiling-failure status can be
  clobbered to `ReviewRequired` (looks like success). Do not "fix Z" by masking — see §8.
- Upstream weakness **X-1:** `RoomExtractor.FindCeilingsForRoom` matches ceilings by XY overlap in a Z band
  `[floorZ−0.5, roomTopZ+5.0]` with a **20 ft fallback and no level filter**, `matchedCeilings[0]` (order,
  not nearest) → a stacked room can inherit the level-below slab's height. **Real but staged (Phase 3).**

## 7. Family Hosting — CURRENT implementation (Decision 011, STATIC/BUILD-VERIFIED; RUNTIME-UNVERIFIED)

`RevitSprinklerPlacementService.PlaceSinglePoint` selects an `IFamilyPlacementStrategy` from the **PROVEN**
`symbol.Family.FamilyPlacementType.ToString()` (never inferred from the family name). Ordered set:
`FaceBasedPlacementStrategy`, `WorkPlaneBasedPlacementStrategy`, `LevelBasedPlacementStrategy`. **No match
→ `UNSUPPORTED_FAMILY_PLACEMENT` failure — never defaulted onto a Level** (hard rules 5,6,7).

- **FaceBased** → needs a host ceiling face (host doc, or linked ceiling via
  `Reference.CreateLinkReference(linkInstance)`). No face → **`REQUIRED_HOST_UNAVAILABLE`** (no fake instance).
- **WorkPlaneBased** (this is what `Sprinkler - Pendent - Hosted` reports):
  1. try ceiling face (prefer downward/underside face, `FaceNormal.Z < -0.5`);
  2. else `SketchPlane` through the requested world XYZ via
     `NewFamilyInstance(sketchPlane.GetPlaneReference(), xyz, XYZ.BasisX, symbol)` — **preserves world Z**;
  3. else **`WORKPLANE_UNAVAILABLE`**. **The Level overload is NEVER used for WorkPlaneBased** (that was the
     (0,0,0) bug).
- **OneLevelBased** → `NewFamilyInstance(xyz, symbol, level, NonStructural)` — correct only for genuinely
  level-based families.
- `referenceDirection` = `XYZ.BasisX` (must lie in the face; `(0,0,1)` would be parallel to a horizontal
  ceiling normal and rejected).

`CeilingHostResolver.FindCeilingHost`: host ceilings first (level-filtered), then linked ceilings
(host-point → link space once; face → host ref via `CreateLinkReference`). Returns `HostFace=null` when none
(no host fabricated).

## 8. Actual-Location Diagnostics & Post-Placement Validation (Decision 011 / §23)

After `strategy.Place`, the service reads `instance.Location as LocationPoint`, computes
`deviationFt = actual.DistanceTo(requested)`, and classifies:
- `PLACED_AND_VALID` if `deviation ≤ PlacementValidationToleranceFt` (**0.5 ft** default);
- `PLACED_BUT_INVALID` otherwise (catches the historic (0,0,0) origin-snap). A returned `ElementId` is
  **not** success on its own (hard rule 8). `SprinklerPlacementResult.Success` requires
  `Failed==0 && PlacedButInvalid==0 && Placed>0`.
- Structured codes in `Strategies/PlacementStrategyContracts.cs → PlacementStatusCodes`
  (`PLACED_AND_VALID`, `PLACED_BUT_INVALID`, `PLACEMENT_FAILED`, `REQUIRED_HOST_UNAVAILABLE`,
  `WORKPLANE_UNAVAILABLE`, `UNSUPPORTED_FAMILY_PLACEMENT`, `LEVEL_RESOLUTION_FAILED`,
  `NON_FINITE_COORDINATES`, `SKIPPED_DUPLICATE`, `REVIT_CREATION_FAILED`, …), copied as **strings** onto the
  Revit-free UI DTOs.

**Actual host/level read-back (Decision 012, added 2026-08-25, STATIC/BUILD-VERIFIED):** on the success
path the service also records, read-only and best-effort, what Revit **actually** created — on
`PlacedSprinklerEntry`: `FamilyPlacementType`, `HostCeilingElementId`, `LinkInstanceName`,
`ActualHostElementId`, `ActualHostName`, `ActualInstanceLevelId`, `ActualInstanceLevelName`,
`ActualScheduleLevelName` (`INSTANCE_SCHEDULE_ONLY_LEVEL_PARAM`). Purpose: distinguish a genuine **hosting
defect** from a pure **view-range / level-association** symptom (the "wrong floor plan" report) in one run.
`ReadActual*` helpers in `RevitSprinklerPlacementService.cs`; each try/catch → null, never affects placement.
 **The export tells the truth about the created instance — actual fields are never back-filled with requested
 coordinates.**

## 8b. Placement Preflight / UI Eligibility (Decisions 013, 014 & 015, 2026-08-26, STATIC-VERIFIED; RUNTIME-UNVERIFIED)

The BruteForce UI blocks rooms the production pipeline provably cannot place. Eligibility is **not** re-derived in
the UI — it is produced by the Backend as a single source of truth, it is a **deterministic feasibility preflight**
(Decision 015 — **not** a live placement probe), and it is a **4-state model** (Decision 014):

- `ISprinklerPlacementService.EvaluateRoomEligibility(RoomUiData room, IReadOnlyList<CalculatedSprinklerPoint> candidates, string familyName, string typeName)`
  (interface in `FireProtection.UI/Services`; UI stays Revit-free).
- **Decision 015 (current):** `RevitSprinklerPlacementService.EvaluateRoomEligibility` runs a **deterministic
  feasibility preflight** that reuses the **same** resolvers real placement uses — `ResolveFamily`
  (symbol + proven `FamilyPlacementType` + strategy), `ResolveHostLevel`, `CeilingHostResolver.FindCeilingHost` —
  but **creates no instance and opens no transaction**. Feasibility by proven placement type: **FaceBased** needs a
  real host **face** on ≥1 candidate (else BLOCKED `NO_CEILING_HOST`); **WorkPlaneBased** is ELIGIBLE once a candidate
  resolves a host level (ceiling face when present, else a `SketchPlane` at the requested Z — always constructible);
  **OneLevelBased** is ELIGIBLE once a level resolves; no candidate resolves a level ⇒ BLOCKED `MISSING_HOST_LEVEL`;
  any preflight exception ⇒ PLACEMENT_ERROR `PREFLIGHT_ERROR`. Results cached per (family/type/room), invalidated via
  `ClearEligibilityCache()`.
- **Why the live probe (Decisions 013/014 mechanism) was replaced:** it decided eligibility by *creating* a
  `FamilyInstance` per candidate inside a rolled-back `Transaction` and requiring the created instance to be spatially
  valid. That coupled eligibility to the placement executor's **runtime** behaviour, so any placement runtime defect
  made **every** room non-eligible → the reported **"0 eligible rooms"** pathology (master prompt §37 = SYSTEM-ERROR
  signal). Decision 015 keeps the 4-state contract but removes the coupling; actual placement correctness is
  re-checked at placement time and guarded again in the Place command (§23).
- **Four states (never collapsed):**
  - **ELIGIBLE** — ≥1 candidate placed + spatially valid. Selectable.
  - **BLOCKED** — deterministic inability only: `MISSING_ROOM_GEOMETRY`, `UNSUPPORTED_FAMILY_PLACEMENT`,
    `UNSUPPORTED_FAMILY_PLACEMENT_TYPE`, `NO_CANDIDATE_POINTS`, `MISSING_HOST_LEVEL`/`NO_CEILING_HOST`
    (host provably absent, e.g. FaceBased `REQUIRED_HOST_UNAVAILABLE`), `NO_PLACEABLE_CANDIDATE`. Not selectable.
  - **PLACEMENT_ERROR** — `CREATED_BUT_INVALID`, `PROBE_EXCEPTION`, or other unexpected API/runtime failure while a
    candidate existed and placement was attempted. A real defect, surfaced distinctly (red outline, dedicated count,
    detail in `Reason`). **Never** reported as BLOCKED. Not selectable.
  - **UNKNOWN** — insufficient evidence: family/type not selected, or the candidate calculation itself could not run.
    Not selectable (amber). Never treated as BLOCKED.
- **Circular-hide break:** the probe's 4-state classification means a broken placement path yields `PLACEMENT_ERROR`
  (visible) for the affected rooms, never a blanket `BLOCKED` that would mask the defect. `RefreshEligibility` sets
  **UNKNOWN for every room** when the candidate calculation throws (rather than probing with a null list → BLOCKED),
  and auto-deselects any room that becomes non-ELIGIBLE. **Error ≠ Blocked; Unknown ≠ Eligible/Blocked.**
- **Why the prior (2026-08-26 first-pass) preflight was insufficient:** it only tested a single representative centroid
  point and treated `WorkPlaneBased` as *always* eligible, without running the real calculation or a real placement
  probe. Runtime proved it false: rooms reported eligible but actual placement produced `Placed=0, INVALID=N, Failed=N`
  (the `(0,0,0)` origin snap — the old Level-overload bug, fixed in Decisions 011/012). Decision 013 made the probe
  authoritative; Decision 014 split the fail-closed outcome into BLOCKED vs PLACEMENT_ERROR vs UNKNOWN so real defects
  stay visible.
- Result DTO `PlacementEligibilityResult` (`FireProtection.UI/Services`, Revit-free) carries
  `EligibilityState / IsEligible / IsBlocked / IsPlacementError / IsUnknown / Reason / StatusCode /
  FamilyPlacementType / HostingStrategy / CeilingSource / LinkInstanceName / HostLevelId / HostLevelName`.
- UI wiring: `SprinklerBruteForceViewModel.RefreshEligibility()` computes candidates for every visible room
  (new `CollectAllVisibleRooms()`), calls the probe, and `RoomItemViewModel.SetEligibility` applies the 4-state result.
  Only ELIGIBLE rooms are selectable (grid checkbox `IsEnabled=IsEligible`, single Select-All toggle selects only
  `IsEligible`, `ApplyDefaultSelection` selects only `IsEligible`). "Show eligible rooms only" filters to `IsEligible`
  (hides BLOCKED/ERROR/UNKNOWN equally). `CollectSelectedRooms` and `CanExecutePlaceSprinklers` guard on
  `SelectedVisibleEligibleRoomCount`, so a non-eligible room can never reach placement. `[ROOM-ELIGIBILITY]` /
  `[ROOM-SELECTION-GUARD]` / `[ROOM-CANDIDATE-DIAGNOSTIC]` `Debug` diagnostics prove the path and pinpoint defects (§17).
- **Status:** UI builds clean (0 errors, 2026-08-26). Backend not buildable in the headless CLI (Revit API `CS0246`,
  environmental); type-correctness verified by inspection against `PlacementContext`, `strategy.Place`,
  `PlacementOutcome`, `LevelResolution`, `CalculatedSprinklerPoint`, `PlacementStatusCodes`. Runtime Revit
  verification **pending** (no Revit host in this environment). The `(0,0,0)`-style defect is already fixed by
  Decisions 011/012; if a residual `CREATED_BUT_INVALID` appears at runtime, the 4-state model surfaces it as
  `PLACEMENT_ERROR` (with requested-vs-actual deviation in `Reason`) rather than `BLOCKED` — to be fixed at the
  placement/coordinate source, **not** by enlarging the validation tolerance.

## 9. Level Resolution (`ResolveHostLevel`, STATIC-VERIFIED)

1. **Direct host id:** `levelId` parses to a host `Document` `Level` → use it.
2. **Linked level:** for each `RevitLinkInstance`, find the `Level` in the link doc (by id, else name),
   transform its elevation to host space (`GetTotalTransform().OfPoint`), then match a **host** `Level` by
   **elevation (±0.01 ft)** or by **name**.
3. **Last resort:** host-doc name match.
4. **Failure:** records `LEVEL_RESOLUTION_FAILED` with full diagnostics (attempted matches, source doc/level/
   elevation) — **no silent/arbitrary level** (Decision 007).

## 10. View-Range concern (keep separate from placement — §24)

Distinct concepts to never conflate: **actual world Z** · **host `Level`** · **`Schedule Level`** ·
**plan-view associated level** · **View Range/View Depth** · **Underlay** · **linked-model visibility**.
A prior session first concluded the "sprinkler in wrong floor plan" was a **View Range** issue (Case C,
static) — that was **later superseded** by runtime evidence proving a real (0,0,0) **placement-overload**
bug (Case B). **Rule: never move coordinates to fix a visibility symptom** (R-10). Visibility diagnosis is
**RUNTIME-only / pending**.

## 11. Build / Test Status

- **Source of truth:** baseline logs in `.analysis/` (`baseline_build_revit2025.log`, `baseline_tests.log`),
  **plus a re-run on 2026-08-25** after the Decision 012 diagnostics change: `dotnet build … -c Revit2025`
  = **0 errors** (2 benign MSB3277-class warnings); `FireProtection.Tests` = **ALL TESTS PASSED**.
- **Revit2025 build:** **0 errors** (`EXIT=0`); log summary "2 Warning(s)". Reference warnings are
  **MSB3277** (Microsoft.VisualBasic 10.0↔10.1, System.Drawing/CefSharp/etc. from multi-targeting net8
  against Revit DLLs) — **environmental, not code defects**. (The audit characterizes these as ~"828
  MSB3277"; the log's final tally line shows 2 — either way: benign reference warnings, 0 errors.)
- **Revit2024 / Revit2026 build:** 0 errors (prior sessions). Revit2025 *bin-copy* was once blocked by a
  VS/Revit file lock on `FireProtection.UI.dll` — **environmental**, not a code error.
- **Tests:** **14/14 PASS** (`ALL TESTS PASSED`, `EXIT=0`) — Revit-free logic only (grid, Z fallback,
  missing/sloped ceiling flags, degenerate boundary, determinism). Run with
  `dotnet run --project FireProtection.Tests/FireProtection.Tests.csproj -c Revit2025`.
- **Runtime Revit placement:** **UNVERIFIED / BLOCKED** — no live Revit host in this environment. All
  runtime acceptance is **pending** (never fabricated — hard rule 18).

## 12. Git / Worktree State (PROVEN, 2026-08-25)

- Branch **`fire-protection-development`** (tracks `origin/…`, up to date). **`main` is behind 3.**
- **Nearly everything is UNCOMMITTED:** the entire refactor **and** all tracking docs **and** both PDFs are
  **untracked or modified-unstaged**. Committed history is trivial (`Initial Fire Protection solution`, then
  textfile churn). **The whole current project state lives in the working tree, not in git history.**
- **Deleted (staged in working tree, mid-refactor):** legacy `Services/Placement/*`
  (`RevitSprinklerPlacer`, `SprinklerPlacementService`, `SnowdonPlacementProvider`, `HostLevelResolver`,
  `GenericSprinklerRuleProvider`, `ISprinklerRuleProvider`, `RevitPlacementExecutor`,
  `SprinklerFamilyResolver`, `SprinklerPlacementRequest`), `Models/Placement/*` (old rules/point/result/
  status), `Models/Snowdon/*`, UI `Services/{IPlacementExecutor,PlacementRequestItem,PlacementRunReport}.cs`,
  and two old master-prompt `.md`s.
- **Added (untracked, the new architecture):** `…/Placement/Sprinklers/**`, `…/Model/SpatialHelpers.cs`,
  UI `Models/Sprinklers/**`, UI `Services/{ISprinklerPlacementService,IPlacementInputExporter}.cs`,
  `Backend/Properties/`, `.analysis/`, and all the tracking `.md`s + PDFs.
- **DO NOT** `git reset`/`clean`/revert. **DO NOT** commit without explicit user go-ahead. If committing is
  requested: branch off `fire-protection-development`, and verify the deletions have no remaining references
  (Phase 10) before removing them.

## 13. Decisions (see `DECISIONS.md` for full text; do not overwrite history)

| ID | Decision | Status |
|---|---|---|
| 001 | Extraction fully separated from calc/UI/placement | Accepted |
| 002 | Linked geometry normalized to host-MEP coords **at extraction** (once) | Accepted |
| 003 | **Backend → UI** dependency only; UI never references Backend | Accepted |
| 004 | Sprinkler spacing uses **provisional 15 ft** placeholder; `HasApprovedRules=false` | Accepted (intentional, not final) |
| 005 | Extraction is read-only, **no transaction** | Accepted |
| 006 | `.addin` registers **only** the Application add-in (command via ribbon button) | Accepted |
| 007 | Linked-model `LevelId`s can't be used directly → resolve via elevation/name | Accepted |
| 008 | Calculated coords already host-MEP feet; placement applies **no** transform | Accepted |
| 009 | Per-room error isolation (extraction + calc) | Accepted |
| 010 | WorkPlaneBased → **Level fallback** | **SUPERSEDED by 011** |
| 011 | **Explicit `IFamilyPlacementStrategy` per proven `FamilyPlacementType`; no WorkPlaneBased→Level fallback; SketchPlane honoring world XYZ; post-placement validation; structured status codes** | **Implemented (STATIC/BUILD-VERIFIED); RUNTIME-UNVERIFIED** |
| 012 | **Placement records ACTUAL host/level read back from the created instance (host id/name, instance level, schedule level) — diagnostics-only, to triage the "wrong floor plan" symptom in one run** | Implemented (STATIC/BUILD-VERIFIED 2026-08-25); RUNTIME-UNVERIFIED |

**Undetermined (per repo):** NFPA-13 compliance path; Collision/Smoke/Notification scope; multi-link
strategy beyond level resolution. **"Snowdon" is NOT an integration** — only sample data
(`extractTest.json`, `TextFile1.txt`). No `Snowdon*` classes exist; treat any such prompt reference as a
template artifact.

## 14. Known Bugs / Concerns

> Severity + anchors from `ARCHITECTURE_AUDIT.md` (static) unless noted. "Anchor" line numbers for the
> calc engine were not re-read this session — trust the audit's provenance, re-confirm before editing.

| ID | Sev | Area | Symptom | Cause | Status |
|---|---|---|---|---|---|
| **(0,0,0)** | Crit | placement | hosted sprinkler at project origin | WorkPlaneBased placed via **Level** overload | **Cause PROVEN (prior-session runtime, Revit 2025)**; **FIXED in code via Decision 011**; **fix RUNTIME-UNVERIFIED** |
| **P-2 / R-03** | High | placement | created-but-wrong reported as success | no post-placement check | **Addressed** by §23 validation (STATIC); runtime-pending |
| **X-1 / R-05** | High | extraction | wrong-level ceiling → wrong Z/floor | no level filter, 20 ft fallback, first-not-nearest | Open (Phase 3) |
| **C-A / R-06** | High | calc | arbitrary Z with >1 FLAT ceiling | unsorted `FirstOrDefault(FLAT)` | Open |
| **C-B / R-07** | High | calc | coverage gaps | `MaxSpacing` used as **min** separation; coverage never verified | Open / **FPE-gated** |
| **C-E** | High | calc | grid coarsening to 30 ft can exceed max spacing; candidate cap leaves gaps | adaptive grid + cap | Open |
| **C-G / R-08** | High | calc | ceiling failure masked as success | `MissingCeiling`/`UnsupportedCeiling` → `ReviewRequired` | Open (Phase 6) |
| **C-C** | Med | calc | boundary clearance ignores inner loops (shafts/atria) | `RoomGeometry` | Open |
| **C-H** | Med | calc | obstacle rejection XY-only, no Z gating → over-reject | `…Service.cs` | Open |
| **I-1 / R-11** | Med | placement | no true idempotency (0.25 ft proximity only; no generation tag) | — | Open (Phase 8) |
| **C-F** | Low | calc | no NaN/finite guard in calc | — | (service now guards finiteness at *placement*) |
| **R-15** | Low | placement | SketchPlane clutter (one plane per point) | new WorkPlane no-face path | New; runtime-confirm |

## 15. Open Tasks (prioritized; exact next action per item)

- **P0 — RUNTIME-verify Decision 011 + 012 in Revit.** Run the add-in in Revit 2025 with host
  `02_FireProtection_Test.rvt` + link `01_Architectural_Test.rvt`, family `Sprinkler - Pendent - Hosted`,
  type `3/4" Pendent on Drop with Guard`. From the exported placement JSON, for Room **1235683** capture and
  check: `ResolvedFamilyPlacementType`; per placed entry `RequestedX/Y/Z` vs actual `X/Y/Z`,
  `PlacementDeviationFt`, `IsSpatiallyValid`, `HostingStrategy`, `CeilingSource`, `HostCeilingElementId`,
  `LinkInstanceName`, `ActualHostElementId`/`ActualHostName`, `ActualInstanceLevelName`,
  **`ActualScheduleLevelName`**.
  - **Pass:** actual ≈ **(14.12, 31.95, 12)**, deviation ≤ 0.5 ft, `PLACED_AND_VALID` count == placed,
    strategy `WorkPlaneCeilingFace`/`WorkPlaneSketchPlane` (never `LevelBased`), **no (0,0,0)** instances.
  - **Triage if it "shows in the wrong plan":** if actual XYZ is correct (deviation small) AND host/level
    look right → **view-range / `ActualScheduleLevelName`** issue, NOT a coordinate bug (do not move coords,
    R-10). If actual host is null/wrong or deviation large → hosting defect; capture the fields above before
    any code change (no speculative strategy swap — hard rule).
  - Delete stray (0,0,0) instances from prior runs first. **BLOCKED here (no Revit host).**
- **P1 —** replace provisional 15 ft with FPE-approved NFPA-13 hazard-specific values (flip
  `HasApprovedRules` only when real tables exist — **FPE-gated**, do not invent); robust sloped-ceiling Z;
  expand tests beyond 14.
- **P2 —** X-1 ceiling-association hardening (level filter + sort nearest-above); C-A/C-G/C-B calc fixes
  (values FPE-gated); implement Collision / Smoke-Detector / Notification-Appliance (currently empty-shell
  ViewModels — no extractor/logic).
- **P3 —** consolidate export-path helpers; quarantine root sample artifacts; Phase 10 commit of staged
  deletions after a reference grep; keep reinforcing the Snowdon non-integration.

## 16. Completed Work (do NOT repeat)

- Extraction pipeline (levels/rooms/ceilings/obstacles/existing) — implemented (static).
- BruteForce calc engine (X/Y grid + Z) — implemented; documented in `SPRINKLER_POINT_CALCULATION_EXPLAINED.md`.
- **(0,0,0) root cause traced to placement overload — PROVEN at runtime (prior session).**
- **Decision 011 strategy pattern implemented** (Face/WorkPlane/Level strategies, `CeilingHostResolver`,
  post-placement validation, structured status codes, requested-vs-actual recording) — STATIC/BUILD-VERIFIED.
- Dead-code cleanup (legacy exporters/overloads/`SnowdonPlacementProvider`/`RevitSprinklerPlacer`, etc.) —
  removed in working tree (deletions staged, uncommitted).
- Full architecture audit + migration plan + risk register + standards matrix — produced.
- Coordinate pipeline traced (single-transform invariant holds); level-resolution path inspected;
  existing-sprinkler detection inspected. **This memory session:** verified code-vs-doc, reconciled drift,
  created `PROJECT_MEMORY.md` + `STANDARDS_MEMORY.md`.

---

# CONTINUATION PROTOCOL

Future sessions MUST:
1. Read **`PROJECT_MEMORY.md`** first, then **`STANDARDS_MEMORY.md`**.
2. Read only the specific source files relevant to the current task (§4 lists them).
3. Trust §0: current-truth docs = Decision 011 + audit-era files + actual source; the stale files' *fix*
   `file:line` refs are obsolete.
4. Check `DECISIONS.md`/`TODO.md`/`SESSION_NOTES.md` only for **updates since this snapshot** (2026-08-25).
5. Do **NOT** reread the whole repo. Do **NOT** reread the PDFs unless verifying a specific rule (then update
   [[STANDARDS_MEMORY]]). PDF 2 is image-only — do not attempt full OCR.
6. Do **NOT** restart prior investigations (§16). Do **NOT** re-diagnose the (0,0,0) bug — it is proven+fixed
   in code, only runtime-confirmation remains.
7. Never invent a standards value (hard rule 4). Never claim runtime success from a compile (hard rule 18).
8. Update this file + record decisions after meaningful architectural/code changes.

# CURRENT TASK

- **Current task:** P0 — physically-correct, **runtime-verifiable** sprinkler placement (Decision 011 + 012).
- **Current phase:** Migration Plan **Phase 5/6 = code-complete, STATIC/BUILD-VERIFIED** (Decision 011
  strategy pattern + Decision 012 actual-host diagnostics); awaiting **runtime verification**.
- **Last completed action (2026-08-25):** Inspected the current placement source; found the post-placement
  diagnostics did not read back the instance's ACTUAL host/level. Closed that gap (Decision 012,
  diagnostics-only): added actual host/level/schedule-level fields to `PlacedSprinklerEntry` +
  `ReadActual*` helpers in `RevitSprinklerPlacementService`. Re-ran build (Revit2025, 0 errors) and tests
  (all PASS). Updated `DECISIONS.md` (012), this file, `SESSION_NOTES.md`, `TODO.md`, `PROGRESS.md`.
- **Next action:** **RUNTIME-verify placement in Revit** (P0, §15) using the enriched diagnostics — the one
  thing impossible in this Revit-free environment. Do **not** make any speculative placement/strategy change
  without runtime evidence. If no Revit host is available, the next static-safe work is Phase 3 (X-1 ceiling
  association) or Phase 9 (tests) — **not** any FPE-gated spacing change.
- **Blocked by:** No live Revit host here (runtime verification BLOCKED). FPE-approved NFPA-13 values
  unavailable (spacing BLOCKED / must not be invented).
- **Files changed this session:** `SprinklerPlacementResult.cs` (+8 diagnostic fields),
  `RevitSprinklerPlacementService.cs` (+`ReadActual*` helpers, success-path population). Docs:
  `PROJECT_MEMORY.md`, `DECISIONS.md`, `SESSION_NOTES.md`, `TODO.md`, `PROGRESS.md`.
- **Last verification:** BUILD-VERIFIED (Revit2025 0 errors, re-run 2026-08-25) · TEST-VERIFIED (all PASS,
  re-run 2026-08-25) · STATIC-VERIFIED (Decision 011 + 012 in source) · **RUNTIME-UNVERIFIED** (no Revit host).

### Update 2026-08-26 — Placement Preflight / UI Eligibility (Decision 013)

- **Completed this session:** Implemented the authoritative pre-placement eligibility layer (single source of
  truth). `ISprinklerPlacementService.EvaluateRoomEligibility` added (UI); `RevitSprinklerPlacementService`
  implements it reusing `ResolveSymbol` → strategy `CanHandle` → `ResolveHostLevel` → `FindCeilingHost`.
  UI `RoomItemViewModel.SetEligibility` + `SprinklerBruteForceViewModel.RefreshEligibility()` apply it,
  auto-deselect blocked rooms, and the grid + Select-All toggle disable blocked rooms. `CollectSelectedRooms`
  guards blocked rooms. DTO `PlacementEligibilityResult` (Revit-free) carries full diagnostics.
- **Verification:** UI **BUILD-VERIFIED** (0 errors, 0 warnings, 2026-08-26). Backend **NOT buildable in this
  headless CLI** (Revit API `CS0246` — environmental); type-correctness verified by inspection against
  `LevelResolution` / `CeilingHostLookup` / `_strategies`. **RUNTIME-UNVERIFIED** (needs Revit).
- **Files changed this session:** `FireProtection.UI/Services/PlacementEligibilityResult.cs` (new),
  `FireProtection.UI/Services/ISprinklerPlacementService.cs`, `FireProtection.UI/ViewModels/Sprinklers/BruteForce/RoomItemViewModel.cs`,
  `FireProtection.UI/ViewModels/Sprinklers/BruteForce/SprinklerBruteForceViewModel.cs`,
  `FireProtection.Backend/Services/Placement/Sprinklers/Final/RevitSprinklerPlacementService.cs`. Docs:
  `PROJECT_MEMORY.md` (§8b), `DECISIONS.md` (013), `PROGRESS.md`, `SESSION_NOTES.md`.
- **Still blocked by:** No live Revit host (runtime verification of preflight + placement BLOCKED).

## Related
[[STANDARDS_MEMORY]] · [[DECISIONS]] · [[ARCHITECTURE_AUDIT]] · [[MIGRATION_PLAN]] · [[RISK_REGISTER]] ·
[[STANDARDS_COMPLIANCE_MATRIX]] · [[PROGRESS]] · [[TODO]] · [[SESSION_NOTES]] ·
[[SPRINKLER_POINT_CALCULATION_EXPLAINED]] · [[PROJECT_CONTEXT]] · [[ARCHITECTURE]]
