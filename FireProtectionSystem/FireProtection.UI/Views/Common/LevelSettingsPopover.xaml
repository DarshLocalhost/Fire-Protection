<Window x:Class="FireProtection.UI.Views.Common.LevelSettingsPopover"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Level Settings"
        Width="420" Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FireProtection.UI;component/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/FireProtection.UI;component/Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Rectangle Width="3" Height="14" Fill="{StaticResource BrandBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="HeaderText" Text="LEVEL SETTINGS" Style="{StaticResource EyebrowText}"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   x:Name="SubHeaderText"
                   Text=""
                   FontSize="11.5"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="2" x:Name="Field1Container" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Field1Label" Grid.Column="0" Text="" VerticalAlignment="Center" FontSize="12"/>
            <ComboBox x:Name="Field1Combo" Grid.Column="1" Style="{StaticResource StandardComboBoxStyle}" IsEditable="False"/>
        </Grid>

        <Grid Grid.Row="3" x:Name="Field2Container" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Field2Label" Grid.Column="0" Text="" VerticalAlignment="Center" FontSize="12"/>
            <ComboBox x:Name="Field2Combo" Grid.Column="1" Style="{StaticResource StandardComboBoxStyle}" IsEditable="False"/>
        </Grid>

        <Grid Grid.Row="4" x:Name="Field3Container" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Field3Label" Grid.Column="0" Text="" VerticalAlignment="Center" FontSize="12"/>
            <ComboBox x:Name="Field3Combo" Grid.Column="1" Style="{StaticResource StandardComboBoxStyle}" IsEditable="False"/>
        </Grid>

        <Grid Grid.Row="5" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Style="{StaticResource OutlineButtonStyle}"
                    Content="Cancel"
                    Click="CancelButton_Click"
                    Padding="18,6"
                    IsCancel="True"/>
            <Button Grid.Column="3"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Content="Apply"
                    Click="ApplyButton_Click"
                    Padding="18,6"
                    IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
